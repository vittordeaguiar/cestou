# Supabase (project: Cestou — sa-east-1)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy fallback accepted by the clients; prefer the publishable key above.
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Server-only — never expose to the browser. Not used by V-43.
SUPABASE_SERVICE_ROLE_KEY=

# DeepSeek (server-only)
DEEPSEEK_API_KEY=

# Firecrawl (server-only)
FIRECRAWL_API_KEY=
